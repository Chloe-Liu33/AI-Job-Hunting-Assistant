# Optional extras — the two *alternative* framework demos that are NOT wired
# into the web UI (they're standalone reference modules / CLI). The base app,
# including the LangChain Agent tab, runs from requirements.txt alone.
# Install only if you want to run these reference implementations locally:
#     pip install -r requirements-extras.txt

# --- LlamaIndex RAG query engine (app/agent_llamaindex.py) ---
llama-index-core>=0.11
llama-index-embeddings-huggingface>=0.3
llama-index-llms-gemini>=0.3
llama-index-llms-groq>=0.2

# --- OpenAI Agents SDK on Groq's OpenAI-compatible endpoint (app/agent_openai_sdk.py) ---
openai-agents>=0.0.10
openai>=1.50
